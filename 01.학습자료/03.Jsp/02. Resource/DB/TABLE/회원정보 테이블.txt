회원테이블
-- 시퀀스 생성
create sequence seq_member_idx;

-- 테이블 생성
create table member(
	idx NUMBER(3) primary key,
	name VARCHAR2(50) not null,
	id VARCHAR2(20) not null unique,
	pwd VARCHAR2(50) not null,
	email VARCHAR2(100),
	addr VARCHAR2(200)	
);

insert into member values(seq_member_idx.nextVal,'홍길동','hong','1111','hong@korea.com','서울시 관악구');

commit;

회원번호	이름 	아이디	비밀번호	이메일	주소	비고
						삭제(버튼)


조회 추가 삭제 수정

﻿
create table member(
id varchar2(20) primary key,
pwd varchar2(50) not null,
name varchar2(50) not null
);

﻿

insert into member values('aaa','1234','aaa');